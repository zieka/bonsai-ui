{
  "name": "bonsai-ui",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://zieka.github.io/bonsai-ui",
  "dependencies": {
    "@maji/react-prism": "^1.0.0",
    "gh-pages": "^1.0.0",
    "node-sass": "^4.5.3",
    "node-sass-chokidar": "^0.0.3",
    "npm-run-all": "^4.1.1",
    "prismjs": "^1.8.3",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-router": "^4.2.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build-css": "node-sass-chokidar src -o src --recursive && npm run build-bonsai",
    "build-bonsai": "node-sass src/assets/bonsai/bonsai.scss -o public/ --output-style compressed",
    "watch-bonsai": "node-sass -w src/assets/bonsai/bonsai.scss -o public/ --output-style compressed",
    "watch-css": "npm run build-css && npm run watch-bonsai && node-sass-chokidar src -o src --watch --recursive",
    "start-js": "react-scripts start",
    "start": "npm-run-all -p watch-css start-js",
    "build": "npm run build-css && react-scripts build",
    "postbuild": "mkdir build/bonsai-ui && mv build/static build/bonsai-ui/static/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
